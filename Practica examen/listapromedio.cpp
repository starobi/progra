#include <iostream>
#include<cmath>
using namespace std;
float promedio(float list[],float u){
    float suma=0,promedio;
    for (float u=0;u<size;u++){
      suma=suma+list[u];
    }
    return promedio =suma/i;

}
main(){
  float array[10],size;
  size=sizeof(list)/sizeof(list[0])
  cout<<promedio(array[], size);
}
